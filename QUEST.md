Задача вот какая: у меня есть веб панель с openAI совместимыми чатами -- использую вместо chatgpt

  но работает она ХУЖЕ чем локальный opencode даже на бытовых задачах. Opencode как и любой coding agent -- сверхоптимизированный harness который разрывает любой другой

  Из чего возникает задача для продукта, который ты тут мне сейчас напишешь.

  У OpenCode есть ... свой режим сервера: https://opencode.ai/docs/en/server/ а у claude code вообще есть agent SDK

  Так что нам нужна "обвязка" вокруг opencode или claude code или еще чего-нибудь, которая позволит запускать его и "выводить наружу" в качестве провайдера для OpenAI (responses или completions API, наверное лучше responses -- так как у нас как раз "server-driven conversations" получаются однозначно)

Я бы наверное предпочёл agent SDK но только с нюансом: в ~/.zshrc есть способ подключить к claude code deepseek -- надо чтобы agent SDK умел так же -- на opus мы разоримся

